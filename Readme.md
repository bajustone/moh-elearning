### RUN DEV SERVER
- STEP 1: create db and app-data directories if not exists
    > `mkdir db app-data`
- STEP 2: Run this:
    > `docker compose up`
- STEP 3: access the dev server at
    > `http://localhost:8088/`